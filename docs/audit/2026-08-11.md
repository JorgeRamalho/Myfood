# Auditoria 2026-08-11

Varredura Playwright em `http://127.0.0.1:5502/frontend/index.html`.

**Score geral (baseline):** 61/100

## Achados críticos

1. Hero de marca fora do first viewport (carrossel no topo)
2. SEO sem Open Graph / Twitter / JSON-LD no HTML estático
3. Carrossel dependente de Unsplash (falhas ORB)
4. Dois frontends no mesmo pacote (Vite + Next)

## Ações aplicadas

- [x] Hero MyFood no topo da Home (Vite; Next arquivado)
- [x] Metas OG / Twitter / JSON-LD em `index.html` e `vite.html`
- [x] 21 imagens do carrossel em `public/media/carousel/`
- [x] `docs/` + ADR frontend canônico (Vite)
- [x] README raiz e backend alinhados
- [x] Next.js movido para `frontend/archive/next/` (ADR-002)
- [x] Raiz enxuta + docs de structure/planning
- [x] Frontend enxuto: app em `src/`, configs em `config/`, assets só em `public/`

## Fase 3 aplicada (qualidade)

- [x] Contraste teal texto `#0F766E` (AA) + fill `#0D9488`
- [x] Lighthouse CI (`lighthouserc.json` + workflow `quality.yml`)
- [x] Smoke e2e Playwright (`scripts/e2e-smoke.mjs`)
- [x] Docs em `docs/quality/`

## Backlog restante (Fase 4+)

- Persistência (Postgres/Prisma)
- Auth real
- PWA
- Avaliar revive do Next só se SSR/PWA justificar
